'use strict';

console.log('index linked');

function verifyPW(str){

  let arr = str.split('');

  let verifyArr = [ 0, 0, 0, 0 ];

  let lc = [ 'a', 'b', 'c'];
  let uc = [ 'A', 'B', 'C'];
  let sc = [ '!', '@', '#'];
  let nu = [ '1', '2', '3'];

  console.log(arr);

  console.log(verifyArr);
  
}